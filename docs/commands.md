# 完整指令表

## 说明

- **权限**：`无` = 普通群友可用；`超级管理员` = 仅 `.env` 中 `SUPERUSERS` 配置的 QQ 号可用
- **范围**：群聊 / 私聊 / 私聊+群聊
- 指令中 `<参数>` 为必填，`[参数]` 为选填
- 同一行列出的多个指令名完全等价（均可触发相同功能）
- `on_command` 指令是否需要 `/` 等前缀由 NoneBot 的 `COMMAND_START` 决定

---

## 对话类

| 指令              | 权限 | 范围 | 参数     | 说明                                                            |
| ----------------- | ---- | ---- | -------- | --------------------------------------------------------------- |
| @Bot 或以昵称开头 | 无   | 群聊；超管私聊需开启 | 对话内容 | 主对话入口；私聊还需 `private_chat_enabled=true`                |
| `ai <内容>`       | 无   | 群聊；超管私聊需开启 | 对话内容 | 快速 AI 助手（需开启 `fastai_enabled`，无角色扮演/分段/表情包） |

---

## 性格类

| 指令                                                       | 权限 | 范围 | 参数     | 说明                       |
| ---------------------------------------------------------- | ---- | ---- | -------- | -------------------------- |
| `性格切换` / `切换性格` / `人格切换` / `切换人格` `<名称>` | 无   | 群聊 | 性格名称 | 将自己的性格切换为指定预设 |
| `查看性格` / `查看人格`                                    | 无   | 群聊 | 无       | 输出全部性格预设及其 prompt |

---

## 模型管理（超级管理员）

| 指令                                             | 范围      | 参数                        | 说明                                                         |
| ------------------------------------------------ | --------- | --------------------------- | ------------------------------------------------------------ |
| `查看可用模型` / `查看模型` `[关键词]`           | 私聊/群聊 | 搜索关键词（选填）          | 列出可用模型，支持多关键词模糊搜索，如：`查看模型 openai 4o` |
| `查看当前配置` / `查看配置`                      | 私聊/群聊 | 无                          | 可视化展示当前大模型各项运行状态与绑定的模型                 |
| `刷新模型` / `刷新模型列表`                      | 私聊/群聊 | 无                          | 重新读取 TOML 并自动拉取各服务商最新模型列表                 |
| `切换模型 <模型名或编号>`                        | 私聊/群聊 | 模型名或编号                | 设置不使用 MoE 时的默认模型，如：`切换模型 1`                |
| `切换moe <难度> <模型名或编号>`                  | 私聊/群聊 | 难度（0/1/2）+ 模型名或编号 | 设置 MoE 各难度对应模型，如：`切换moe 0 2`                   |
| `切换视觉模型` / `设置视觉模型` `<模型名或编号>` | 私聊/群聊 | 模型名或编号                | 设置视觉任务专用模型                                         |
| `切换分类模型` / `设置分类模型` `<模型名或编号>` | 私聊/群聊 | 模型名或编号                | 设置 MoE 开启时难度、视觉和候选工具分类使用的模型            |
| `切换总结模型` / `设置总结模型` `<模型名或编号>` | 私聊/群聊 | 模型名或编号                | 设置工具包复核等总结任务使用的模型                           |

---

## 功能开关（超级管理员）

| 指令                                     | 范围      | 参数              | 说明                                 |
| ---------------------------------------- | --------- | ----------------- | ------------------------------------ |
| `设置moe <开关>`                         | 私聊/群聊 | `0`/`1`/`开`/`关` | 是否启用混合专家调度模式             |
| `设置联网` / `切换联网` `<开关>`         | 私聊/群聊 | `0`/`1`/`开`/`关` | 是否启用网络搜索；还需开启工具调用且模型支持 Function Calling |
| `设置工具调用` / `设置函数调用` `<开关>` | 私聊/群聊 | `0`/`1`/`开`/`关` | 是否启用函数调用（Function Calling） |
| `设置私聊 <开关>`                        | 私聊/群聊 | `0`/`1`/`开`/`关` | 是否允许超级管理员私聊 Bot           |

---

## 工具与插件管理（超级管理员）

| 指令                                 | 范围      | 参数     | 说明                                                                                      |
| ------------------------------------ | --------- | -------- | ----------------------------------------------------------------------------------------- |
| `刷新工具` / `重载工具` / `刷新插件` | 私聊/群聊 | 无       | 原子重载 NoneBot 插件描述、文件/已激活生成工具及启用的 MCP 工具                              |
| `插件黑名单` / `查看插件黑名单`      | 私聊/群聊 | 无       | 查看禁止 LLM 调用的插件/函数/MCP 工具列表                                                 |
| `添加插件黑名单 <工具标识>`          | 私聊/群聊 | 工具标识 | 禁止 LLM 调用指定 NoneBot 插件、自定义函数或 MCP 工具                                     |
| `移除插件黑名单 <工具标识>`          | 私聊/群聊 | 工具标识 | 从黑名单移除指定工具                                                                      |
| `添加常驻插件 <标识>`                | 私聊/群聊 | 工具标识 | 将 NoneBot 插件、自定义函数或 MCP 工具设为常驻                                            |
| `添加常驻函数 <标识>`                | 私聊/群聊 | 函数名   | 将 custom_tools 中的函数设为常驻                                                          |
| `移除常驻插件 <标识>` / `移除常驻函数 <标识>` | 私聊/群聊 | 工具标识 | 从常驻列表移除                                                               |
| `常驻插件` / `查看常驻插件` / `查看常驻函数` / `查看常驻工具` | 私聊/群聊 | 无 | 查看常驻工具列表                                                          |

### 工具标识说明

工具标识可以是：

- NoneBot 插件包名：如 `nonebot_plugin_xxx`
- 自定义函数名：如 `extract_webpage`
- MCP 工具名：如 `mcp__danbooru_searcher__search_tags`
- MCP 服务级黑名单：如 `mcp__danbooru_searcher`
- MCP 通配黑名单：如 `mcp__danbooru_searcher__*`

示例：

```text
添加插件黑名单 mcp__filesystem
添加插件黑名单 mcp__filesystem__read_file
添加插件黑名单 mcp__filesystem__*
移除插件黑名单 mcp__filesystem
添加常驻插件 mcp__danbooru_searcher__search_tags
```

---

## 上下文管理

| 指令                                                                  | 权限       | 范围      | 参数 | 说明                                                  |
| --------------------------------------------------------------------- | ---------- | --------- | ---- | ----------------------------------------------------- |
| `重置我的` / `重置对话` / `清空上下文` / `清空对话`                   | 无         | 私聊/群聊 | 无   | 清空自己的上下文及 CD 状态（群聊中需 @Bot）           |
| `重置全部对话` / `重置所有对话` / `清空所有上下文` / `清空全部上下文` | 超级管理员 | 私聊/群聊 | 无   | 清空所有用户个人上下文及所有群聊记忆（群聊中需 @Bot） |

---

## 运行中请求管理（超级管理员）

| 指令                                                       | 范围      | 参数          | 说明                                                       |
| ---------------------------------------------------------- | --------- | ------------- | ---------------------------------------------------------- |
| `查看请求` / `查看当前请求` / `当前请求` / `查看正在请求`   | 私聊/群聊 | 无            | 查看当前正在处理的 LLM 请求、来源、用户、运行时长与消息预览 |
| `停止请求` / `终止请求` / `取消请求` / `stop请求` `[参数]` | 私聊/群聊 | 编号或 `all`  | 停止指定或全部正在处理的 LLM 请求；不带参数且仅有一个请求时会直接停止 |
| `重载LLM` / `刷新LLM` / `重载llm` / `刷新llm`            | 私聊/群聊 | 无            | 原子重载全部运行资源；失败继续使用旧 generation              |
| `查看LLM状态` / `LLM状态` / `查看llm状态` / `llm状态`    | 私聊/群聊 | 无            | 查看队列、拒绝、缓存、分类、工具、投递模式和最近重载错误      |

### AI 工具包热插拔（超级管理员）

| 指令 | 参数 | 说明 |
| ---- | ---- | ---- |
| `添加LLM功能 <需求>` / `创建LLM功能 <需求>` | 功能需求 | 当前聊天模型生成草稿，隔离测试后由总结模型复核；不会自动启用 |
| `查看LLM功能草稿 [ID]` | 草稿 ID（选填） | 无 ID 时列出草稿；有 ID 时展示权限、风险、diff、源码预览和哈希 |
| `批准LLM功能 <ID> <短哈希>` | 草稿 ID + 至少 8 位哈希 | 仅允许批准复核通过且内容哈希未变化的草稿 |
| `拒绝LLM功能 <ID>` | 草稿 ID | 标记拒绝并保留源码供审计 |
| `LLM功能列表` | 无 | 查看活跃版本和草稿状态 |
| `停用LLM功能 <工具包>` | 工具包 ID | 发布不含该工具包的新 generation |
| `回滚LLM功能 <工具包> <版本>` | 工具包 ID + 版本哈希前缀 | 原子切回唯一匹配的历史只读版本 |

“热插拔”只涵盖工具包，不卸载 NoneBot Matcher/Hook。普通用户不能查看或执行 `superuser` 工具，也不存在临时执行 Python 的普通用户入口。runner 的系统要求与网络/文件系统边界见[自定义工具开发](./custom-tools.md#runner-隔离边界与运行要求)。

**停止请求参数：**

| 格式 | 示例          | 含义                     |
| ---- | ------------- | ------------------------ |
| 省略 | `停止请求`    | 仅有一个运行中请求时停止它；多个请求时提示先查看编号 |
| 编号 | `停止请求 1`  | 停止编号为 1 的请求      |
| 全部 | `停止请求 all` | 停止所有正在处理的请求   |

被停止的请求会清理对应用户的 CD 状态。若请求已经进入流式输出或工具执行阶段，已经发出的消息或已经执行的工具副作用不会被撤回。

---

## Token 消耗查询（超级管理员）

| 指令                                            | 范围      | 参数               | 说明                    |
| ----------------------------------------------- | --------- | ------------------ | ----------------------- |
| `查看消耗` / `查询token` / `token消耗` `[参数]` | 私聊/群聊 | 数量或范围（选填） | 查看 API Token 消耗记录，新记录会显示本轮请求累计耗时 |

**参数格式：**

| 格式 | 示例             | 含义                          |
| ---- | ---------------- | ----------------------------- |
| 省略 | `查看消耗`       | 最近 5 条                     |
| 数字 | `查看消耗 10`    | 最近 10 条                    |
| 区间 | `查看消耗 10-15` | 第 10 到第 15 条（1-indexed） |
| 负数 | `查看消耗 -50`   | 最早的 50 条                  |

新版本产生的消耗记录会在模型名后显示耗时，例如：

```text
[1] 05-22 16:58:00 | gpt-4o-mini | 耗时 12.3s
 ├ 提示词: 1234 (其中命中缓存: 500)
 ├ 生成:   233
 └ 总计:   1467
```

这里的耗时为本轮 LLM 请求从登记开始到该次 API usage 被记录时的累计耗时；旧记录没有耗时字段时会保持原样。

---

## 相关页面

- [NoneBot 插件集成](./plugin-integration.md) — 黑名单与常驻插件的使用场景
- [自定义工具开发](./custom-tools.md) — 常驻函数的使用场景
- [性格系统](./personality.md) — 性格指令详细说明
- [配置参考](./configuration.md) — 各功能的配置文件参数
