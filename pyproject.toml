[tool.poetry]
name = "nonebot-plugin-moellmchats"
version = "0.25.0"
description = "A Nonebot plugin, Multi-Model Chat System | Hybrid Scheduling·Search Augmented·Persona Customization"
license = "GPL"
authors = ["Elflare", "LoCCai Maintainers"]
maintainers = ["LoCCai Maintainers"]
readme = "README.md"
homepage = "https://github.com/LoCCai/nonebot-plugin-moellmchats"
repository = "https://github.com/LoCCai/nonebot-plugin-moellmchats"
documentation = "https://github.com/LoCCai/nonebot-plugin-moellmchats#readme"
keywords = ["nonebot", "nonebot2", "llm", "ai", "chat"]

[tool.poetry.dependencies]
python = "^3.10"
aiohttp = ">=3.14.0,<4.0.0"
nonebot2 = ">=2.4.4,<3.0.0"
nonebot-adapter-onebot = ">=2.4.6,<3.0.0"
ujson = ">=5.12.0,<6.0.0"
nonebot-plugin-localstore = "^0.7.0"
python-dotenv = ">=1.2.2,<2.0.0"
tomli = { version = "^2.0.1", python = "<3.11" }
mcp = {extras = ["cli"], version = ">=1.28.1,<2.0.0"}
[tool.poetry.group.dev.dependencies]
pytest = "^8.3.0"
pytest-asyncio = "^0.24.0"
ruff = "0.16.2"
nonemoji = "^0.1.2"
pre-commit = "^4.0.0"
filelock = ">=3.20.3,<4.0.0"

[tool.ruff]
line-length = 130
target-version = "py310"
extend-exclude = ["tests/.data"]

[tool.ruff.format]
line-ending = "lf"

[tool.ruff.lint]
select = [
    "F",     # Pyflakes
    "W",     # pycodestyle warnings
    "E",     # pycodestyle errors
    "I",     # isort
    "UP",    # pyupgrade
    "ASYNC", # flake8-async
    "C4",    # flake8-comprehensions
    "T10",   # flake8-debugger
    "T20",   # flake8-print
    "PYI",   # flake8-pyi
    "PT",    # flake8-pytest-style
    "Q",     # flake8-quotes
    "TID",   # flake8-tidy-imports
    "TC",    # flake8-type-checking
    "RUF",   # Ruff-specific rules
]
ignore = [
    "E402",   # module-import-not-at-top-of-file
    "UP037",  # quoted-annotation
    "RUF001", # ambiguous-unicode-character-string
    "RUF002", # ambiguous-unicode-character-docstring
    "RUF003", # ambiguous-unicode-character-comment
    "TC003",  # runtime annotations intentionally import stdlib collection protocols
]

[tool.ruff.lint.isort]
force-sort-within-sections = true

[tool.ruff.lint.pyupgrade]
keep-runtime-typing = true

[tool.pyright]
pythonVersion = "3.10"
pythonPlatform = "All"
defineConstant = { PYDANTIC_V2 = true }
executionEnvironments = [{ root = "./" }]

typeCheckingMode = "standard"
reportShadowedImports = false
disableBytesTypePromotions = true

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
