# Revision directory

Revisions form one linear PostgreSQL history. F-04 starts it with `0001_users_conversations`; F-05 appends `0002_agent_runtime`; F-06 appends `0003_agent_steps`; F-07 appends `0004_tool_calls`.
